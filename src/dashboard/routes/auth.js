const router = require('express').Router();

const client = require('../utils/karma');

/* |=======================================|
 * |               ROUTE · "/"             |
 * |=======================================|
 */

router.get('/redirect', (req, res) => {
	//
});

module.exports = router;
